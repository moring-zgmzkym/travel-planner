# TripMate 启动指南

> 手把手带你把项目跑起来，全程约 5 分钟。（2026-09-12 起为**多用户版**：需注册登录，见第 8 节）

## 0. 前置条件

| 依赖 | 要求 | 检查命令 |
|---|---|---|
| Python | 3.12（必须） | `python --version` |
| Node.js | ≥ 18（仅 12306-MCP 真实通道需要，没有也不影响运行） | `node --version` |
| 网络 | 能访问 LLM 服务即可；攻略/图源/天气不可达时自动降级，不阻塞 | — |

## 1. 安装依赖（二选一）

**方式 A：使用实训共享虚拟环境（推荐，本项目开发时用的就是它）**

虚拟环境位于项目上级目录 `..\..\.venv`（即 `实训项目\.venv`），依赖已装好，可跳过本步。

**方式 B：新建虚拟环境**

```bash
cd 大作业/TripMate
python -m venv .venv
.venv\Scripts\activate          # Windows
pip install -r requirements.txt
playwright install chromium     # PDF 路书渲染引擎（约 150MB，一次；也可自动改用系统 Edge）
```

> **PDF 渲染说明（2026-09-06 起）**：定稿路书为「唐风夜色」HTML 排版 → 无头 Chromium 打印。
> 本机没有 playwright 浏览器时自动改用系统 Edge；两者都不可用或渲染异常时自动降级为
> reportlab 卡通模板并在时间线提示。`.env` 加一行 `PDF_RENDERER=reportlab` 可完全回到
> 旧渲染（应急开关）。

## 2. 配置 .env（唯一必改的文件）

项目根目录已有 `.env`（当前配置：**主模型 = 智谱 GLM glm-5.3-flash**，次级 = OpenCode Zen mimo-v2.5-free 自动兜底）。

主模型连续失败（限流/断网）时会自动切换到次级模型，主模型恢复后 2 分钟内自动切回，无需人工干预。
若需更换服务商（如硅基流动千问系列），改主模型三行即可：

```ini
LLM_API_KEY=你的Key
LLM_BASE_URL=https://api.siliconflow.cn/v1
LLM_MODEL=Qwen/Qwen2.5-72B-Instruct
```

次级模型由 `LLM_FALLBACK_API_KEY / LLM_FALLBACK_BASE_URL / LLM_FALLBACK_MODEL` 三行控制，删掉即关闭自动切换。其余 Key（`TAVILY_API_KEY`、`AMAP_API_KEY` 等）**全部可选**——不配置时对应功能自动走降级通道并在界面上标注"参考值"，不影响启动和演示。逐项说明见 `.env.example`。

## 3. 启动

```bash
# 方式一：双击 run.bat（Windows，已配置 UTF-8 编码）

# 方式二：命令行
python run.py
```

看到类似日志即启动成功：

```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
```

浏览器打开 **http://127.0.0.1:8000**——右上角连接点变绿表示 WebSocket 已连通。

## 4. 跑通验收用例（企划书 §10.1）

把下面这段话整句粘贴进聊天框发送：

> 帮我规划十一成都 3 天游，10 月 1 号从上海出发，高铁往返，两个人，预算 6000 最多 7000，想休闲一点顺便吃吃喝喝，酒店想住春熙路附近 300 到 500 一晚的，必去大熊猫基地。

预期流程（全程约 3-8 分钟，取决于模型速度）：

1. 聊天管家回复确认信息、启动规划团队；
2. 右侧时间线实时滚动各 Agent 进度（攻略搜索 / 车票酒店查询并行）；
3. 聊天窗出现**草稿卡片**（逐日行程表 + 预算表）；
4. 可继续对话修改（如「第 2 天换成都江堰」）或回复「确认」；
5. 确认后产出 **PDF 行程计划 + 推荐订单清单**（左侧订单面板可点直达链接）。

## 5. 测试与冒烟（可选）

> **要改代码？** 先读根目录的 `文件修改注意事项.md`（每个文件的作用 + 改它会波及哪些文件的索引与速查表）；
> 用 AI 助手改代码时见 `AGENTS.md`。

```bash
python -m pytest tests/ -q           # 228 项单元测试
python scripts/e2e_step4_finalize.py # 定稿→PDF 冒烟（不消耗 LLM，可随时跑）
python scripts/e2e_step2_collect.py  # 四 Agent 协同冒烟（消耗 LLM）
```

## 6. 常见问题

| 现象 | 原因与处理 |
|---|---|
| `429 FreeUsageLimitError` / `Rate limit exceeded` | LLM 通道限流。智谱主模型限流时会自动切到 OpenCode 次级（日志可见）；持续失败可按第 2 步换服务商 |
| `未配置 LLM_API_KEY` | `.env` 不在项目根目录，或 Key 拼写有误 |
| 时间线一直没动静 | 多为 LLM 通道限流/断连，看 `logs/tripmate.log` 里的 ERROR；系统护栏会在模型协议执行不完整时自动补齐流程 |
| 界面打开是空白 | 确认服务已启动且访问的是 `http://127.0.0.1:8000`（不是 https） |
| 车票显示"降级参考值" | 12306-MCP 为社区实现，可能随官方接口变动失效；系统已自动降级并标注，PDF 生成不受影响 |
| PDF 里图片是示意卡片 | Tavily/Wikimedia 图源均不可达时的降级表现（混排时部分实拍部分示意，逐图标注） |

## 7. 停止

命令行按 `Ctrl + C` 即可。产出的 PDF 与配图保留在 `outputs/` 目录，运行日志在 `logs/tripmate.log`（含 ReAct 思考→行动→观察审计链路，答辩可作证据）。

## 8. 手机访问（本机当服务器，手机当客户端）

电脑开机跑着服务，手机连**同一个 Wi-Fi** 即可当随身客户端用：

1. `.env` 里把 `HOST=127.0.0.1` 改为 `HOST=0.0.0.0`，重启服务；
2. Windows 防火墙放行 8000 端口（首次启动弹窗选"允许"，或手动放行入站规则）;
3. 手机浏览器打开 `http://<电脑局域网IP>:8000`（IP 用 `ipconfig` 查看 IPv4 地址）；
4. 手机浏览器菜单选"**添加到主屏幕**"——已配置 PWA manifest，会得到独立图标、全屏启动，接近原生 App 体验。

> ⚠️ **安全须知**：同一 Wi-Fi 内的任何人都能打开注册页（多用户版为开放注册）。
> 仅限家庭/个人热点等可信局域网使用，**不要做公网端口映射**；不需要手机访问时把 HOST 改回
> `127.0.0.1`。电脑关机/睡眠后手机端即不可用（数据与产出都在电脑上，不受影响）。
> 手机端页面已做竖屏单栏适配（≤700px 断点）。

## 9. 多用户与账号（2026-09-12 新增）

- **注册/登录**：首次打开页面会弹出登录卡，切到"注册"建号即可；**第一个注册的用户自动成为管理员**，
  并自动继承升级前的旧会话数据（聊天记录、画像不丢）。
- **会话隔离**：每个账号的对话、画像、PDF 完全独立，互相不可见；偏好记忆也按账号分开。
- **偏好记忆**：每次规划**定稿后**，系统自动用 LLM 从画像提炼长期偏好（风格/预算习惯/酒店偏好/
  忌口/节奏，≤12 条），左侧"🧠 偏好记忆"面板可查看、单条删除、一键清空；下次规划与聊天会自动
  参考这些偏好（你本次明示的要求永远优先）。
- **PDF 分享**：成品卡片上的"🔗 生成分享链接"会产出一个免登录直链（`/share/xxx`，链接即凭证），
  发给任何人都能下载这份 PDF；可"撤销"使其立即失效；同一会话同一份 PDF 重复点击复用同一链接。
- **忘记密码**：让管理员在项目目录执行（PowerShell）：
  ```powershell
  ..\..\.venv\Scripts\python.exe -c "from tripmate import auth; auth.admin_reset_password('用户名', '新密码')"
  ```
- **数据位置**：账号 `data/users.json`、分享 `data/shares.json`、记忆 `data/memory/`——均已加入
  `.gitignore`，请勿把 `data/` 分享给他人（含密码哈希）。
- **成本提示**：开放注册意味着同网段注册者都会消耗你配置的 LLM 额度；只把服务开在可信网络里。
- **outputs 清理**：每次规划会产出 PDF 与景点图片，长期累积。服务**每次启动会自动清理**一次
  （只删确定无引用且超过保护窗的文件；每个会话当前定稿的 PDF、分享中的 PDF、画像引用的图片**永久保留**；
  被取代的旧 PDF 保留 3 天、无引用图片保留 24 小时）。也可手动执行：
  ```powershell
  ..\..\.venv\Scripts\python.exe scripts\cleanup_outputs.py            # 预览将删清单（不删）
  ..\..\.venv\Scripts\python.exe scripts\cleanup_outputs.py --apply    # 执行删除
  ```
