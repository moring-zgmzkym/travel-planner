# TripMate 文件修改注意事项

> 本文件是 TripMate 项目的**文件索引与跨文件改动手册**，回答两个问题：
> ① 每个文件是干什么的；② 改这个文件会连带影响哪些代码/文件。
>
> 覆盖范围：全部 106 个非生成文件（源代码、前端、脚本、测试、配置、数据、文档）。
> 所有跨文件契约均标注 `file:line` 证据，关键数字（测试数、配置项、分区名）经直读/grep 核实。
> 最后核对日期：2026-09-23。

---

## 1. 这份文档怎么用

**改代码前（30 秒规则）**：

1. 在第 4 节找到你要改的文件，读它的"文件卡"，特别是**修改注意点**；
2. 如果你的改动涉及任何**共享词汇**（见第 3 节：WS 消息、黑板分区、通道名、标记词、主题名、工具返回键、配置项），再去查第 2 节速查表；
3. 改完后对照第 5 节的**分类检查清单**逐项打勾，再跑相关测试（第 4.9 节有"测试 → 覆盖模块"对照表）。

**给一句话结论**：本项目的联动风险几乎全部集中在少数几组**跨文件重复定义的字符串**上——它们没有编译器保护，改漏了不会报错，只会静默出错。这就是本文档存在的原因。

---

## 2. 高危耦合点速查表（改 X 必须同步改 Y）

| # | 改动 | 必须同步检查的地方 |
|---|---|---|
| 1 | WS 出站消息字段 | `status.py:59-74`（emit 事件结构）→ `static/app.js:271-317`（handleMsg）→ `test_chat_delivery_persistence.py`、`test_frontend_state.py`、`test_resilience_hardening.py` |
| 2 | `models.TravelProfile` 字段（增删改名） | `blackboard.py` apply_* → `persistence.py` 快照 → `maintenance.py` collect_referenced 引用扫描 → 前端 `renderProfile`（app.js）→ `pdf_html/context.py` → `pdf_templates/base.py` → `templates/draft.html` → 十余个测试 fixture |
| 3 | 状态机步骤名 / 协议标记词 | `team.py:50-68` 的 13 键 SPEAKER 表 ↔ `team.py` `_selector` 兜底 if/elif 分支 ↔ `prompts.py` 各 Agent 流程描述 ↔ `test_selector.py` |
| 4 | 8 个 subagent 通道名（guides/covers/foods_img/spots_img/tickets/hotels/weather/route） | `team.py` `_sub_notify` 调用点 ↔ `subagents.py` `run_channel_subagent(channel=…)` ↔ `static/index.html:68-75` 灯位 ↔ `app.js` 的 `SUB_PARENT`/`SUB_CHANNELS` ↔ 会话级 `sub_states` 键 ↔ `planning.FIELD_IMPACT` 中的通道键 |
| 5 | PDF 主题名（lushu/elegant/executive） | `pdf_html/themes.py` ↔ `static/app.js:376,555,556`（默认值来源/本地存储/发送）↔ 网关 `/api/templates`（app.py:233-241）↔ `test_pdf_themes.py` ↔ 旧会话 `basic_info.template` 经 `resolve_theme` 静默回退 |
| 6 | MCP 工具名关键词（`("ticket",)`/`("searchhotels",)`/`("text","search")`/`("detail",)`/`("around",)`/`("direction",*)`） | 分散在 `tools/tickets.py:47`、`hotels.py:125,181,197`、`route.py:109,124,161,180,260`；测试桩在 `test_route.py`、`test_resilience_hardening.py`。改关键词 = 改 MCP 工具匹配行为，必须跑这两个测试 |
| 7 | 工具返回 dict 的键 | `team.py` 各收割函数逐键取值（`:295-330`、`:460-471`、`:499-521`、`:546-626`、`:660-710`）↔ `models.py` `TicketCandidate(**t)`/`HotelCandidate(**h)` 逐键构造 ↔ `mocks/data.py` 对应 mock 键集 |
| 8 | PDF 产物命名 `行程计划_{目的地}_{run_id[:8]}.pdf` | `pdf_html/context.py:34` ↔ `pdf_templates/base.py:219` ↔ `maintenance.py` 引用扫描保护 ↔ 网关 `_final_pdf_basename`（app.py:245-258）↔ `test_pdf_html.py:194` |
| 9 | 前端 DOM id/class | `static/index.html` ↔ `static/style.css`（文件头注释：选择器契约与 app.js 对齐）↔ `app.js` 的 `$()/querySelector`。新增通道灯要三处同步 |
| 10 | 配置项增删 | `config.py`（`_env("X")`）→ `.env.example` → README / 启动指南配置表 → 测试 monkeypatch 的**类属性名**（如 `BudgetConfig.TOKEN_LIMIT`） |
| 11 | 占位图来源文案 `_PLACEHOLDER_SOURCE`（search.py:30） | PDF 两侧按文案过滤实拍图：`pdf_html/context.py:79` 与 `pdf_templates/base.py:147,568` 匹配"示意/非实景/占位"关键词——改文案必须同改这两处判据 |
| 12 | 改动 `static/` 下任何文件 | `index.html:10`（`style.css?v=2`）与 `index.html:119`（`app.js?v=4`）的**手工缓存击破版本号**要递增，否则用户端不更新 |

**一句话记忆法**：改"数据结构"（models/blackboard/persistence）看第 2 行；改"字符串协议"（WS/标记词/通道/主题）看第 1、3、4、5 行；改"外部集成"（MCP/工具）看第 6、7 行。

---

## 3. 共享词汇表（跨文件字符串契约字典）

以下词汇在本项目多处重复定义，**是全项目最贵的字符串**。改动前先在这里确认全量出现位置。

### 3.1 WebSocket 消息（前后端契约）

**客户端 → 服务端**（app.js 发送，`gateway/app.py:668-700` 分发）：

| type | 字段 | 前端 |
|---|---|---|
| `chat` | `text` | app.js:162 |
| `ping` | `ts` | app.js:130 |
| `stop` | — | app.js:514 |
| `template` | `name` | app.js:381,556 |

握手 URL：`/ws?sid={sid}&ticket={ticket}`；未认证关闭码 **4401**。

**服务端 → 客户端**（app.js:271-317 `handleMsg` 分发）：

| type | 字段 | 发送方 |
|---|---|---|
| `sub_states` | `states{channel: state}` | app.py:597 |
| `memory` | 无字段（偏好沉淀完成信号 → 前端刷新记忆面板；2026-09-23 新增，修"面板停在暂无沉淀"竞态） | `_capture_memory` 推送（app.py） |
| `pong` | `ts` | app.py:661 |
| `status` | `seq, kind, agent, text, ts` + kind 相关附加字段 | `status.py:59-74` |
| `chat` | `role`(user/chatter/system), `text`, `replay`(补播时) | app.py:451,670,685；补播 :590 |
| `draft` | `html`（首推另带 `draft`, `budget`） | app.py:444-447 |
| `final` | `pdf_url, orders[], total_price` | app.py:453-456 |
| `profile` | `profile`（TravelProfile dump 去掉 changelog） | app.py:507,593,699 |
| `usage` | `usage{prompt_tokens, completion_tokens, total_tokens, limit}` | app.py:457；llm.py:387-396 |
| `session` | `sid, title` | app.py:592 |
| `AGENT_MESSAGE` | 直达 type | team.py:1556 |

**STATUS kind 全集**（css 有专属样式的是 AGENT_MESSAGE）：`STATUS_PROGRESS`（默认）、`STATUS_INFO`、`STATUS_PHASE`（附 `phase/eta_min`，team.py:1188）、`STATUS_CHECKPOINT`、`STATUS_DRAFT`、`STATUS_COLLECT`、`STATUS_MCP`、`STATUS_IMAGES`、`STATUS_SUBAGENT`（附 `channel/state`）、`STATUS_COMPLETED`（附 `pdf_url`）、`STATUS_CANCELLED`、`STATUS_ERROR`、`STATUS_FALLBACK`、`AGENT_MESSAGE`。`STATUS_PROGRESS` 心跳还附 `elapsed_s`（team.py:1051）。

### 3.2 黑板分区名（= `models.TravelProfile` 字段名）

数据分区 18 个：`basic_info, detail_info, guide_digest, tickets, hotels, weather, images, spot_notes, food_notes, cover_images, routes, guide_extras, plan_input, draft, draft_feedback, final`；
元字段 4 个：`version, updated_at, run_id, changelog`（不可由工具写入）。

**WriterName**（6 个，models.py:272）：`chatter / processor / researcher / booking / planner / system`。`user_changes_since` 只统计 `writer=="chatter"` 且分区 ∈ {basic_info, detail_info}（护栏自写不触发检查点，刻意设计）。

changelog 的 `field` 记**裸键名**：basic 直接记键；detail 内 hotel 子键记 `hotel.xxx`（blackboard.py:183）；`defaults_applied`/`template` 被检查点过滤（team.py:1576-1578）。

### 3.3 团队协议词汇（team.py 内三处 + prompts.py 镜像）

- Agent 名：`InformationProcessor / Researcher / BookingButler / Planner`（team.py:44-47）；前端徽章另有 `Chatter / TeamRunner`。
- 状态机步骤 13 键（team.py:50-64）：`PROC_BROADCAST, RES_START, MCP_START, RES_COLLECT, MCP_COLLECT, PROC_SUMMARIZE, PLAN_DRAFT, PLAN_IMGREQ, RES_IMG, MCP_ROUTE, ROUTE_COLLECT, PLAN_PDF, DONE`。
- 阶段初始步（team.py:65）：`collect→PROC_BROADCAST`、`revise→PLAN_DRAFT`、`finalize→PLAN_IMGREQ`。
- 完成标记（team.py:67-68）：`DRAFT_READY`（collect/revise）、`FINAL_PDF`（finalize）。
- 群聊消息标记词（prompts.py 与 team.py `_selector` **双重定义**）：`TASK_BROADCAST, SEARCH_STARTED, SEARCH_RESULT, MCP_QUERIES_STARTED, ORDER_RECOMMEND, PLAN_REQUEST, IMAGE_REQUEST, IMAGE_RESULT, ROUTE_STARTED, ROUTE_RESULT`。

### 3.4 subagent 通道名与灯态

通道 8 个：`guides, covers, foods_img, spots_img, tickets, hotels, weather, route`（前端 `index.html:68-75` 的 `data-channel` 与之一一对应）。
灯态 3 个：`running / done / failed`（team.py:253-258）。

### 3.5 主题与模板名

| 名字 | 归属 | 说明 |
|---|---|---|
| `lushu` / `elegant` / `executive` | HTML 主路径（pdf_html/themes.py） | 需配齐 css + cover/back 模板 + 12 章 titles |
| `cartoon` | reportlab 降级路径（pdf_templates） | 唯一注册模板；`classic` 版式已逐字并入其 `_ClassicLayout` |
| `classic` | 已废弃的旧名 | `resolve_theme("cartoon"/未知)` 一律回退 `lushu`；`get_template("classic")` 报错后回退 cartoon |

### 3.6 工具返回结构（统一形态）

所有查询工具返回 dict 均含 `mode`（real/mock/mixed）、可选 `notice`、以及负载键：`candidates`（tickets/hotels）/ `items`（images）/ `digest`（guides）/ `days`（weather）。tickets 另有 `transport_kind`（estimate/flight/train）。
`compute_budget(profile, draft)` 返回键：`items, spot_items, total, budget, budget_max, occupancy, warnings`（planning.py:117-122）。
TeamRunner 回执：`start → {status: accepted|rejected, run_id?}`；`submit_feedback → {status, phase, round?}`；`cancel → {status: cancelled|idle}`。

### 3.7 运行期 JSON 文件 schema

| 文件 | 结构 | 读写方 |
|---|---|---|
| `sessions/{用户名}/{sid}.json` | `{saved_at, chat_history:[{type,role,text}], profile: TravelProfile(无 changelog)}` | persistence.py / session.py |
| `data/users.json` | `{users: {名称: {salt,hash,is_admin,created_at}}}` | auth.py（fail-closed） |
| `data/shares.json` | `{share_id: {user,sid,file,created_at,revoked}}` | app.py:305-313 ↔ maintenance.py:129 |
| `data/memory/{用户名}.json` | `{preferences:[{id,text,updated_at}], trips:[{destination,dates,total_price,finalized_at}]}` | memory.py ↔ app.js:224-234 |
| 令牌/票据 | `{username}:{exp}:{sig}` base64url 无 padding；令牌 30 天、WS 票据 30 秒 | auth.py:151-177 |

### 3.8 环境变量（config.py 实测 27 项）

LLM：`LLM_API_KEY, LLM_BASE_URL, LLM_MODEL, LLM_MAX_TOKENS, LLM_TEMPERATURE, LLM_TIMEOUT_S, LLM_FALLBACK_TIMEOUT_S, LLM_MAX_RETRIES, LLM_FALLBACK_API_KEY, LLM_FALLBACK_BASE_URL, LLM_FALLBACK_MODEL`；
搜索：`TAVILY_API_KEY`；
MCP：`AMAP_API_KEY, AMAP_MCP_URL, MCP_12306_COMMAND, MCP_HOTEL_URL, MCP_HOTEL_TOKEN, MCP_CALL_TIMEOUT_S, MCP_HANDSHAKE_TIMEOUT_S`；
天气：`WEATHER_BASE_URL, WEATHER_GEO_URL`；
预算/行为：`TOKEN_BUDGET, JOB_TIMEOUT_S, ROUTE_BUDGET_S, ALLOW_MOCK_FALLBACK, PDF_RENDERER`；
服务：`HOST, PORT`。
注：`McpConfig.TIMEOUT_S/RETRIES/RETRY_DELAY_S` 已声明弃用（config.py:70-74），mcp_client 不再读取。

---

## 4. 文件索引

路径均相对 `TripMate/`。每张卡片五段：**作用 / 上游依赖 / 下游被依赖 / 跨文件契约 / 修改注意点**。"下游"里标注了主要调用方， grep 符号名可找到全部。

### 4.1 入口与配置

#### `run.py`（12 行）
- **作用**：进程入口，强制 stdout/stderr UTF-8 后委托网关 `main()`。
- **上游**：无。**下游**：`gateway/app.py:main`。
- **注意点**：几乎无耦合；启动方式若变，需同步 `启动指南.md`、`scripts/ws_status_smoke.py` 的启动假设。

#### `run.bat`（6 行）⚠️
- **作用**：Windows 双击启动：设 UTF-8 代码页 → cd 到脚本目录 → 用 `..\..\.venv\Scripts\python.exe` 跑 run.py。
- **注意点**：**硬编码了仓库外的共享 venv 路径**（相对本仓库是上两级）。换机器/移植/重建环境时必须改这一行；README 的"上级目录共享 venv"说法即指此。改它不影响任何测试。

#### `requirements.txt`（19 行）
- **作用**：依赖清单（autogen 0.7.5 / fastapi / uvicorn / websockets / mcp / httpx / jinja2 / playwright / pymupdf / reportlab / pillow / pytest 等）。
- **注意点**：2026-09-06 路线 A 转正时新增 playwright + pymupdf（见 `docs/pdf-template-plan.md`）。新增 import 新三方库时必须同步；`mcp` SDK 版本影响 `mcp_client.py:128` 的 `isError/is_error` 双属性兼容判断。

#### `.env.example` / `.env`
- **作用**：配置模板 / 真实配置（后者含密钥，`.gitignore` 已排除，**任何文档不得引用其内容**）。
- **契约**：键名与 `config.py` 逐个对应（见 3.8 节）。`.env.example` 已于 2026-09-23 补齐 `MCP_HOTEL_TOKEN`、`WEATHER_GEO_URL`、`PDF_RENDERER`。
- **注意点**：新增 `_env("X")` 必须同步 `.env.example` 与两份文档的配置表。

#### `.gitignore`（11 行）
- **作用**：排除 `.env`、`logs/`、`outputs/`、`sessions/`、`data/`、`learning/`、缓存目录。
- **注意点**：`data/users.json`、`sessions/*.json` 含账号哈希与聊天记录，属隐私数据，不入库是刻意设计；新增顶层目录时评估是否该忽略。

#### `docs/pdf-template-candidates.md` / `pdf-template-plan.md` / `pdf-template-tasks.md`
- **作用**：PDF 模板改造的调研、方案与任务清单，含两段决策记录（2026-09-01 定路线 B；2026-09-06 评审触发回退条件，**路线 A：HTML→Chromium 转正**，reportlab 收缩为仅 cartoon 降级）。
- **注意点**：这是"为什么代码长这样"的史料。读 `pdf_html/` 或 `pdf_templates/` 有疑问时先查这里的决策记录，避免重新发明已否定的方案。

---

### 4.2 `tripmate/` 核心模块

#### `tripmate/__init__.py`、`agents/__init__.py`、`gateway/__init__.py`（均空）
- **作用**：包命名空间标记。`agents/` 是预留目录（README 表格注明"预留"），实际无 Agent 实现，Agent 都在 `team.py`/`chatter.py`/`subagents.py`。
- **注意点**：往里加导出符会立刻影响全项目的 `from ... import` 语义，需全量回归。

#### `tripmate/config.py`（140 行）——集中配置
- **作用**：环境变量注入 + 导入即创建 5 个目录 + 8 个配置类。
- **上游**：dotenv。**下游**：几乎全部模块（auth/app_window/digest/maintenance/memory/persistence/llm/status/pdf_gen/pdf_templates/team/session/gateway/tools 全家）。
- **契约**：目录常量 `BASE_DIR/OUTPUT_DIR/IMAGE_DIR/LOG_DIR/SESSIONS_DIR/DATA_DIR`（config.py:13-19）；`ServerConfig.STATUS_REPLAY=80`（补播上限）、`HEARTBEAT_S=30`（心跳，前端 ping 25s 与其配合）；`ALLOW_MOCK_FALLBACK`；`PDF_RENDERER`。
- **修改注意点**：① 测试 conftest.py 对 4 个**模块属性**做 monkeypatch（`pdf_html.context.OUTPUT_DIR`、`pdf_templates.base.OUTPUT_DIR/CROP_DIR`、`imagegen.IMAGE_DIR`），改这些常量的引用方式会让测试隔离静默失效、污染真实 outputs；② 默认值与 `.env.example`、README 三处同步；③ `PDF_RENDERER` 被 pdf_gen 消费，改名/改值 = 换默认渲染路径。

#### `tripmate/models.py`（272 行）——全部 pydantic 模型
- **作用**：`TravelProfile` 及 20 个分区模型（黑板/持久化/PDF/前端同源）。
- **上游**：pydantic。**下游**：blackboard、planning、chatter、digest、memory、session、team、pdf_gen、tools.route、pdf_html、pdf_templates + 几乎全部测试与脚本。
- **契约**：字段名 = 黑板分区名 = 持久化 JSON 键 = 前端 profile 渲染字段 = PDF 模板数据源（**五处同源**）；`BasicInfo.missing_required()` 判"出发地/目的地/游玩天数"三要素，是 start_planning 启动判定、默认值补齐、session 兜底的共同判据；`template` 字段已废弃但旧会话兼容（models.py:29 注释）；`WriterName` 是 Literal 类型契约。
- **修改注意点**：① 任何字段增删改名联动 8 处（见速查表第 2 行）——本项目最贵的改动，改前必读速查表；② pydantic 校验模式决定 LLM 脏数据容错（`coerced_field` 的探测法要求字段可单独 validate）；③ `GuideDigestItem` 的 raw 字段（source_name/source_url/fetched_at/raw_answer/raw_titles/raw_urls）有 `test_guide_digest.py` 专门守护，删字段会连锁失败。

#### `tripmate/blackboard.py`（244 行）——共享黑板
- **作用**：唯一状态载体：串行化写、版本号 + changelog、合并式分区更新、持久化钩子、紧凑 JSON 视图。
- **上游**：models。**下游**：session、chatter、team；测试 10+ 个文件。
- **契约**：`write(section, value, writer, reason, field="")`；`apply_basic_info/apply_detail_info(updates, writer, reason)`；分区名 = TravelProfile 字段；`section_version` 是 `SectionReadyTermination`（team.py:115-）阶段完成判据；`compact_json()` 裁 changelog 至 10 条喂提示词；`on_change` 钩子（session 注册用于落盘）。
- **修改注意点**：① `apply_*` 两段式校验（先 model_copy 校验再落写）防"changelog 已追加而分区未写"，test_blackboard 覆盖；② `clear_sections` 不走锁（team.start 受理点调用），语义必须与 write 一致；③ `user_changes_since` 只认 chatter 且限两个分区（3.2 节）；④ changelog 无限增长风险：compact_json 只裁展示，落盘裁剪在 session 侧。

#### `tripmate/session.py`（360 行）——会话装配
- **作用**：单会话 = 黑板 + 状态总线 + Chatter + TeamRunner + team_events 队列 + 聊天历史/持久化 + 用户消息处理。
- **上游**：blackboard、chatter、models、persistence、status、team、config。**下游**：gateway/app.py（核心）；`test_chat_delivery_persistence/test_frontend_state/test_second_planning/test_new_features/test_relay_view/test_tool_call_guard/test_stability_hardening/test_multiuser`；`e2e_step1/2/3/shashan` 脚本。
- **契约**：`team_events` 元组 `("draft_ready", Draft)/("completed", FinalDelivery)/("error", str)`（:91-97，网关消费）；`sub_states: dict[channel→state]`（会话级灯态真值，网关读写）；`profile_snapshot()` 返回去掉 changelog 的画像（补播/持久化/引用扫描共用同一形状）；`chat_history` 消息 dict = `{"type":"chat","role","text"}`，`record_chat` 恰好一次语义。
- **修改注意点**：① 三组兜底正则 `_START_INTENT/_FEEDBACK_INTENT/_USER_MODIFY_INTENT` 基于"模型宣布启动但未真调工具"的实测失败模式，改动必须过 `test_tool_call_guard.py`；② `_TOOL_MARKUP`（:25）与 `chatter.py:304-305` 的 `clean_reply` 正则是**重复定义**，改一处必须同步另一处（同文件/跨文件双冲突点）；③ 落盘单飞（`_dirty/_flush_task`）是"规划期几百次黑板写不卡 WS"的关键，改动过 test_chat_delivery_persistence；④ `handle_user_message` 的固定失败文案（"（系统提示）…请重发"）被测试断言；⑤ `relay_team_event` 绝不可抛异常（曾烧掉推送协程，:345-370）；⑥ 类级默认属性（username/memory_dirty/sub_states，:78-80）被测试 `__new__` 构造的裸实例依赖，新增实例属性必须有类级兜底。

#### `tripmate/chatter.py`（356 行）——聊天 Agent
- **作用**：唯一用户入口 Agent：5 个工具的装配 + `stream_chatter` 运行/审计 + LLM 脏输入清洗与默认值补齐。
- **上游**：prompts、blackboard、llm、models、status、team。**下游**：session（build_chatter/ensure_travel_dates/stream_chatter/clean_reply）；`test_new_features/test_mocks_data/test_stop_dates/test_relay_view/test_tool_call_guard`；e2e 脚本。
- **契约**：5 个工具名 `save_travel_info/get_travel_profile/start_planning/submit_draft_feedback/stop_planning`——session 正则、tools_seen 判定、prompts.CHATTER_PROMPT 字段清单**四处一致**；`stream_chatter(chatter, user_text, source="user", seen_tools=None)`；`_profile_view` 输出键（draft_summary/draft_budget/guide_highlights/final_summary）。
- **修改注意点**：① 工具签名/docstring 直接约束 prompts.CHATTER_PROMPT 与测试；② `clean_reply` 与 session `_TOOL_MARKUP` 重复（见 session 卡）；③ `ensure_travel_dates` 被 session 确定性启动路径旁路调用（session.py:299），改动影响"无日期启动"行为。

#### `tripmate/team.py`（1607 行）——规划团队（核心，最高风险文件）
- **作用**：四 Agent（SelectorGroupChat）编排 + 确定性 selector 状态机 + JobBoard 并行收集 + 检查点增量重跑 + 护栏补齐 + TeamRunner 契约体。
- **上游**：prompts、blackboard、config、llm、mocks.data、models、digest、pdf_gen、planning、status、tools.*（hotels/tickets/weather/search/route/resilience）、subagents。**下游**：session、chatter（类型注解/clean_reply 反向）、subagents；测试：`test_selector/test_collection_workers/test_blackboard/test_pdf/test_second_planning/test_stop_dates/test_resilience_hardening/test_route/test_zombie_team/test_regression_fixes/test_new_features`；`e2e_step4_finalize`（含私有符号 TeamContext/TeamState/_deliver_final）。
- **契约**：全部见第 3 节（SPEAKER 13 键、标记词、8 通道、工具回执、黑板分区、`_deliver_final` 签名、FIELD_IMPACT 与 changelog 裸 field 名的映射、`_changed_fields` 过滤规则）。
- **修改注意点（同文件内冲突高发区）**：① `SPEAKER` 表 ↔ `_selector` 的八组兜底 if/elif（:1439-1491）↔ prompts.py 流程描述，三处必须同步，漏一处 selector 空转；② 标记词字符串常量与 `_stream_team` 的 clean_reply 清洗（:1542-1560）耦合，改清洗规则会让 `_recover_draft_from_texts` 失效；③ 各通道收割函数逐键解析工具返回（速查表第 7 行）；④ `planning.FIELD_IMPACT` 键 ↔ changelog 裸 field（含 `hotel.xxx` 前缀形式）；⑤ `TokenBudgetExceeded` 六处 `except ... raise`——熔断异常绝不可被降级链吞掉；⑥ JobBoard 900s 预算 + `cancel_with_grace`/`register_abandoned` 是僵尸治理关键路径（test_zombie_team 覆盖）；⑦ spot_notes/food_notes 必须同生同灭（护栏以二者配对为幂等条件，:285-286）。

#### `tripmate/status.py`（105 行）——状态总线 + 审计日志
- **作用**：`StatusBus`（订阅队列 + 环形缓冲 + seq 单调序号）与 `AuditLog`（ReAct 四元组，只进日志不进推送）。
- **上游**：config（LOG_DIR）。**下游**：chatter、team、session、gateway、subagents、memory、persistence、maintenance、pdf_gen、pdf_html/engine；测试多文件。
- **契约**：emit 事件 dict 结构 = WS 出站消息本体（3.1 节全部 kind 值）；`replay_limit=80` 来自 ServerConfig.STATUS_REPLAY；`last_seq()` 是补播去重基线。
- **修改注意点**：① 改 emit 字段 = 改前后端契约（速查表第 1 行）；② `STATUS_SUBAGENT` 的 channel/state 与 team `_sub_notify`、前端 setSubLight 三元契约；③ seq 单调性 + "先订阅后补播"顺序（app.py:634-643）是防丢/防重两个不变量；④ 新增 kind 要考虑前端默认分支（STATUS_PHASE 触发 resetSubLights、终态触发隐藏 ETA chip，app.js:293/490）。

#### `tripmate/llm.py`（379 行）——LLM 客户端工厂 + token 熔断
- **作用**：OpenAI 兼容客户端惰性单例 + `_FallbackClient` 主备自动切换 + per-run token 基线与熔断。
- **上游**：config。**下游**：chatter、team、digest、subagents、memory、gateway（usage_summary）；测试 test_llm_fallback/test_stability_hardening/test_new_features；脚本 probe_max_tokens/e2e_step3/shashan。
- **契约**：`usage_summary() → {prompt_tokens, completion_tokens, total_tokens, limit}`（网关 usage 消息 → 前端 renderUsage）；`check_budget(baseline)` 抛 `TokenBudgetExceeded`；`snapshot_usage()` per-run 基线（`TeamRunner._usage_baseline`，多会话隔离靠它）。
- **修改注意点**：① 主备切换/无文本重试逻辑（:171-186）是 429/空 content 事故的根治点，两个测试硬覆盖；② `reset_usage()` 仅兼容入口，误把熔断改成模块级计数会破坏多会话隔离；③ `_MODEL_INFO`（无 vision/structured）影响新模型接入；④ usage 字段名是前端硬编码。

#### `tripmate/prompts.py`（174 行）——全部系统提示词
- **作用**：Chatter + 4 个团队 Agent + ReAct 规则 + subagent 提示 + 协议标记词。
- **上游**：无（纯字符串）。**下游**：chatter、team、subagents。
- **契约**：字段清单（basic_info/detail_info 可含字段）/工具名/标记词与 team.py `_selector` 一一对应；`SUBAGENT_PROMPT` 的 `{channel}` 占位符由 subagents.py:85 `.format(channel=...)` 填充。
- **修改注意点**：① 改标记词 = 改 selector 协议（速查表第 3 行）；② 改字段清单 = 改 chatter 工具 docstring 约束，三处一致；③ 提示词说"工具无参/调用即完成"实际约束了 research/booking 工具签名。

#### `tripmate/planning.py`（159 行）——规划纯逻辑
- **作用**：变更影响分析 FIELD_IMPACT、预算核算、草稿硬校验、节奏常量。
- **上游**：mocks.data、models。**下游**：team、chatter（懒加载）、gateway（compute_budget）、pdf_html/context、pdf_templates/base；测试 test_planning/test_blackboard/test_collection_workers；e2e_step2。
- **契约**：`compute_budget` 返回键（3.6 节，PDF 两侧 + draft.html 共用）；`FIELD_IMPACT` 键与 changelog 裸 field 名对齐；`PACE_SPOTS` 与 `_fallback_draft`、PLANNER_PROMPT"快 4-5 个/天"三处口径一致。
- **修改注意点**：① FIELD_IMPACT 增删字段直接影响检查点是否重查（有回归测试）；② compute_budget 键名被 PDF/前端模板消费，改名动三处；③ validate_draft 的错误文案是 LLM 自纠输入。

#### `tripmate/digest.py`（343 行）——攻略笔记/锦囊提炼
- **作用**：LLM 提炼景点/美食笔记、攻略原文结构化、定稿前路书锦囊提炼。
- **上游**：config、llm、models、**tools.search（跨层私有导入 `_IMG_HEADERS/_WATERMARK_HOSTS`）**。**下游**：team；tools.route（懒加载 `_parse_json_block`）；test_collection_workers；脚本 probe_*。
- **契约**：`build_digest_notes → {"spots":[SpotNote],"foods":[FoodNote]}`；`structure_guide_rows(rows, city) → list[dict]`（键 spots/foods/routes/warnings/raw_answer）；`build_guide_extras → GuideExtras`；三处 LLM 调用统一走 `_LLM_JSON_ARGS`（effort=low + max_tokens 16384）。
- **修改注意点**：① `_LLM_JSON_ARGS` 的 extra_body 是硅基流动特有参数，换 provider 要重测；② spot_notes/food_notes 必须同生同灭；③ mock 攻略键集与真实 digest 键集**不一致**（spot/foods/routes/warnings vs raw_answer/raw_titles/raw_urls），由 team.py:309 `structure_guide_rows` 吸收——改任一侧必须查 team 解析与 GuideDigestItem 转换；④ `_bigram_hits` 软校验防编造，放松会让幻觉进 PDF。

#### `tripmate/subagents.py`（104 行）——通道 subagent
- **作用**：把 JobBoard 任务升级为独立 AssistantAgent（单工具、最多 2 次尝试，失败降级直调查询函数）。
- **上游**：llm、prompts、status。**下游**：team（8 通道全经此）；test_subagents。
- **契约**：`run_channel_subagent(channel, instruction, query, model_client=None, notify=None)`；产出必须是 dict（`_EXPECT_TYPE`，list 必须包装，team.py:277 注释）；`notify(state)` 取值 running/done/failed。
- **修改注意点**：① dict 产出契约若放宽，team 各 `_query` 包装的注释即失效；② 重试与 resilience 抛弃机制叠加，改次数改变 900s 预算节奏；③ notify 回调异常必须吞掉（灯态不炸主流程）。

#### `tripmate/auth.py`（198 行）——账号/令牌/WS 票据
- **作用**：PBKDF2 口令、HMAC 无状态令牌（30 天）、30 秒 WS 票据、fail-closed 账号库、用户名硬校验。
- **上游**：config（DATA_DIR）。**下游**：memory（validate_username）、gateway（verify_token/verify_ws_ticket/issue_ws_ticket/register/login/user_info）；test_multiuser。
- **契约**：令牌格式 `{username}:{exp}:{sig}` base64url；登录返回 `{username, token, is_admin}`；401 语义（HTTP 401 ↔ WS close 4401 ↔ 前端弹登录）；用户名是 `sessions/{用户名}/` 与 `data/memory/{用户名}.json` 的路径成分。
- **修改注意点**：① 用户名规则与 Windows 保留名黑名单是**路径安全前提**，放松校验 = 路径穿越风险；② `secret.key` 丢失 = 全体令牌失效（无害设计）；③ `verify_*` 绝不抛异常的契约被网关依赖；④ fail-closed 是刻意设计，改"按空库重建"会造成账号抢占。

#### `tripmate/memory.py`（184 行）——跨会话偏好记忆
- **作用**：`data/memory/{用户}.json` 读写、定稿后 LLM 提炼偏好、提示词注入。
- **上游**：auth、config、llm、models、status。**下游**：session（懒加载 prefs_prompt_text）、team（Planner 注入）、gateway（`/api/memory` 端点 + 定稿沉淀）；test_multiuser。
- **契约**：文件结构见 3.7 节（前端 refreshMemory 消费同一键名）；`prefs_prompt_text` 空串 = 提示词零变化；`memory_dirty` 标志触发下次消息锁内热重建 chatter。
- **修改注意点**：① preferences 的 id 由 `DELETE /api/memory/{pref_id}` 消费；② trips 上限 20 是前端 slice(0,5) 前提；③ 提炼失败必须返回 False 不写盘；④ 用户名必须先过 auth.validate_username。

#### `tripmate/persistence.py`（78 行）——会话落盘
- **作用**：`sessions/{用户名}/{sid}.json` 原子读写（tmp + os.replace）。
- **上游**：config、status。**下游**：session、gateway（safe_sid 用于会话键/PDF url/share sid）；test_chat_delivery_persistence。
- **契约**：文件 JSON 键（3.7 节）；`profile` = model_dump 去掉 changelog；无 username 的扁平路径是旧测试/直调兼容分支；`maintenance.collect_referenced` 反向解析该文件结构。
- **修改注意点**：① `safe_sid` 三分支（空→default / 合法→原样 / 非法→`junk-{md5[:8]}`）的 junk 键策略是跨来源隔离的关键设计，不可改回 default 兜底；② 改文件键名要 persistence/session/maintenance 三处同步。

#### `tripmate/maintenance.py`（205 行）——outputs 清理
- **作用**：启动时清理确定无引用的产物（PDF 3 天/散图 24h/crops/baseline/tmp/旧日志），fail-closed。
- **上游**：config、status。**下游**：gateway（lifespan 调用 + SHARES_PATH 别名再导入）、scripts/cleanup_outputs.py、test_maintenance。
- **契约**：引用扫描读会话文件（final.pdf_url/pdf_path、hotels.image_path、images、cover_images）+ 深度扫描兜底；shares.json 条目字段；`no_sessions_guard`（零会话文件跳过 PDF 清理）；`MaintenanceAborted`。
- **修改注意点**：① 深度扫描是防误删兜底，**收紧会误删新字段图片**——新增图片字段优先靠深度扫描而非改这里；② fail-closed 语义不可放宽；③ 与网关 lifespan 调用参数（默认保护窗）耦合。

#### `tripmate/app_window.py`（81 行）——应用窗口弹出
- **作用**：服务就绪后拉起 Edge `--app` 独立窗口（回退默认浏览器），daemon 线程。
- **上游**：config。**下游**：gateway/app.py:main（唯一运行时调用方）；test_frontend_state（TRIPMATE_NO_WINDOW 行为）。
- **契约**：`TRIPMATE_NO_WINDOW=1` 环境变量（测试/ws_status_smoke 约定）；`_display_url` 把 0.0.0.0 归一化为 127.0.0.1。
- **修改注意点**：失败只记日志；`open_app_window_async` 必须保持非阻塞（uvicorn 启动前调用）。

---

### 4.3 `tripmate/gateway/`

#### `tripmate/gateway/app.py`（707 行）——FastAPI 网关（多用户版）
- **作用**：认证路由、会话注册表、REST 端点、`/ws` WebSocket 主循环、`_sender`/`_chat_worker`/`_replay_snapshot` 推送协程、PDF/分享/记忆端点、启动清理钩子。
- **上游**：auth、config、llm、maintenance、memory、planning、session、status、persistence、pdf_html.themes、pdf_templates、app_window（main 内）。**下游**：run.py；`test_multiuser/test_frontend_state/test_chat_delivery_persistence/test_pdf_themes/test_new_features/test_relay_view`（大量直接 import 后改 `app.sessions` 等模块级可变状态打桩）。
- **契约**：HTTP 路由 15 条（12 类）（`/api/register|login|me|ws-ticket|sessions(GET/POST)|profile|usage|memory(GET/DELETE/DELETE{id})|share(POST/DELETE)|templates|pdf`）；WS 主循环（先 `bus.subscribe()` 再 `_replay_snapshot` 补播，按 `last_seq` 起 `_sender`）；会话键 `"用户名/sid"`（`_sid_of` 按 `split("/",1)` 解析）；`team_events` 消费；`_render_draft_html` 读 `templates/draft.html`；`/api/pdf` 支持 query 传 token（`<a>` 标签无法带 Authorization 头）。
- **修改注意点**：① WS 出站字段改名必同步 app.js handleMsg 与三测试（速查表第 1 行）；② "迟到回复送达"依赖 `_live_ws` 活动连接语义，改 `_route_ws`/`_cancel_current_chat` 会破坏 test_chat_delivery_persistence 多个用例；③ `_sender` 协作式交接（`_sender_task`/60s idle）+ 先订阅后补播是防丢防重双不变量；④ 会话键格式级联 `list_sessions`；⑤ `_render_draft_html` 失败被分级捕获（:583-606），新增卡片类型沿用同样降级策略；⑥ 新增模块级可变状态要考虑测试隔离；⑦ `/api/profile`、`/api/usage` 前端从不调用（仅测试断言 401 保护），是潜在死端点。

#### `tripmate/gateway/__init__.py`（空）
- **作用**：包标记。`from tripmate.gateway import app` 依赖它（test_multiuser.py:13）。

---

### 4.4 `tripmate/tools/`（9 个文件）

#### `tools/__init__.py`（空）
- 包标记，无导出；全项目都是 `from .tools.xxx import`。

#### `tools/mcp_client.py`（439 行）——MCP 协议基座
- **作用**：MCP 客户端（stdio/SSE/Streamable HTTP 三传输）+ 阶段级持久会话池（**ContextVar 单例，多用户隔离**）。
- **上游**：config、resilience。**下游**：team（begin/end_mcp_pool 包住 _run_phase）、tickets/train_session、hotels/hotel_session、route/amap_session；test_multiuser/test_regression_fixes/test_resilience_hardening。
- **契约**：`McpSession`/`PersistentMcpSession`；`_match_tool` **运行时 list_tools 发现 + 关键词全命中**（不硬编码工具名，关键词在调用方）；`_filter_args` 按 input_schema 顶层过滤；`begin_mcp_pool/end_mcp_pool`（:159/:164）；`isError/is_error` 双属性兼容（:128）。
- **修改注意点**：① 会话池是 ContextVar，阶段上下文外 create_task 的任务看不到池——team 的 JobBoard/护栏依赖此设计；② 新增 MCP 通道 = config 加配置 + 加工厂 + 工具里调用，三处联动；③ SDK 升/降级复查 :128 双属性判断。

#### `tools/resilience.py`（109 行）——容错基座
- **作用**：外部调用统一超时/重试/取消宽限/抛弃登记。
- **上游**：无（仅 stdlib）。**下游**：mcp_client、search、hotels、tickets、weather、team（急停路径）、test_resilience_hardening/test_subagents。
- **契约**：`ServiceUnavailable`（统一"可降级"信号，上层按通道捕获）；`with_retry(fn, timeout_s=30, retries=2, delay_s=5)`；`cancel_with_grace(task, 5s)`；`register_abandoned(task)`；退避上限 45s。
- **修改注意点**：**调用方显式传超时是历史事故修复**——tickets.py:53、hotels.py:129/183/199 显式传 `McpConfig.CALL_TIMEOUT_S`，weather 传 WeatherConfig.TIMEOUT_S，route 自带 Budget。改 with_retry 默认 timeout 语义前必读这三处注释。

#### `tools/search.py`（419 行）——Tavily 检索 + 图源
- **作用**：8 路攻略检索、逐景点搜图、城市封面图（Wikimedia 兜底 + PIL 占位）。
- **上游**：config、mocks.data、imagegen、resilience。**下游**：team、hotels（私有常量导入）、digest（私有常量导入）、test_new_features/test_regression_fixes/test_frontend_state/test_real_channel_paths。
- **契约**：`search_guides → {mode, digest, notice?}`（digest 行键：source_name/source_url/fetched_at/raw_answer/raw_titles/raw_urls/reference_only）；`search_images → {mode, items:[{spot,path,source}], notice?}`；`search_city_covers → list[str]`（team 包成 `{"covers": [...]}`）；`_guide_queries` 8 路纯函数。
- **修改注意点**：① `_PLACEHOLDER_SOURCE` 文案 = PDF 两侧实拍图过滤判据（速查表第 11 行）；② 8 路查询扩容改变 team.py:301 提示文案；③ `_tavily` 被测试 monkeypatch，勿改签名；④ Commons 熔断 `_COMMONS_DISABLED` 有测试断言。

#### `tools/tickets.py`（199 行）——12306 车票
- **作用**：12306-MCP 车票查询 + 打分勾选 + 自驾/大巴/飞机估算降级。
- **上游**：config、mocks.data、mcp_client、resilience。**下游**：team（`score_tickets`）；test_planning/test_regression_fixes/test_resilience_hardening。
- **契约**：四种形态统一 `{mode, notice?, candidates, transport_kind}`；candidate 键 = `models.TicketCandidate`（train_no/depart_time/arrive_time/duration_min/price/link/source/reference_only/from_telecode/to_telecode）；`score_and_select`（0.5 时间 + 0.3 价格 + 0.2 历时）；`GAS_PRICE_PER_KM=0.8`/`BUS_PRICE_PER_KM=0.35`（测试 import）。
- **修改注意点**：mock_train_tickets 无 telecode（模型有默认值）；改 candidate 键需同步模型与 team 构造；显式传 timeout（见 resilience 卡）。

#### `tools/hotels.py`（371 行）——酒店 + 距离
- **作用**：酒店 MCP（Dida）+ 高德距离 + 打分勾选 + 需求 6 富化。
- **上游**：config、mocks.data、mcp_client、resilience、search（私有常量）。**下游**：team（`score_hotels`）；tools/route.py（**`_haversine` 私有跨模块复用**）；test_planning/test_regression_fixes。
- **契约**：`query_hotels → {mode, notice?, candidates}`；candidate 键 = `models.HotelCandidate`（+ 打分后 score/selected/reason，+ distance_estimated/rating_estimated）；`_normalize_dida` 的内部键 `_pos` 仅函数内消费（:158 pop，不透出）；`score_and_select`（0.4 价格 + 0.3 距离 + 0.3 评分）。
- **修改注意点**：① 改 `_haversine` 签名会打断 route.py:27 的 import；② estimated 标志影响打分中性权重与 PDF"估算标注"；③ 关键词 `("searchhotels",)` 必须唯一切中（:123-126 注释：宽泛词会误中排在前面的其他工具）。

#### `tools/weather.py`（161 行）——Open-Meteo 天气
- **作用**：真实预报（免 Key）+ 降级模拟 + 确定性穿搭/emoji。
- **上游**：config、mocks.data、resilience。**下游**：team（near_term_dates 兜底 travel_dates）、**pdf_html/context.py 与 pdf_templates/cartoon.py 都 import 其 outfit_advice/weather_emoji**；test_real_channel_paths/test_weather_dates。
- **契约**：真实/降级返回同构 `{city, source, reference_only, days:[{date,day_text,temp_min,temp_max}]}`；`near_term_dates` 预报窗 [明天, +15 天]。
- **修改注意点**：outfit_advice/weather_emoji 的输出格式被两套 PDF 链共享，改一处两条链同时变。

#### `tools/route.py`（437 行）——每日路线规划
- **作用**：高德 MCP 每日路线（坐标→饭点餐厅→段间交通→导航链接），全确定性、绝不抛。
- **上游**：config、models、mocks.data、hotels（_haversine）、digest（懒加载 _parse_json_block）、mcp_client（懒加载 amap_session）。**下游**：team；test_route/test_regression_fixes。
- **契约**：`compute_route_plan(prof, session_factory=None) → {"days":[RouteDay dumps], "notice?}`；`review_route_plan`/`merge_review`（LLM 审查，失败返回 {}）；`_nav_url`（uri.amap.com/navigation，省略 from）；`_Budget` 总预算 = RouteConfig.BUDGET_S；常量 `_DIRECTION_KEYWORDS`（route.py:40-42）。
- **修改注意点**：① `RouteDay/RouteStop/RouteSegment` 字段名是 PDF 两侧（context.py:277-308 与 base.py:428-502 `route_day_block`）的共同解析对象；② `route_day_block` 与 context.py `_seg_text` 是**双份维护**，改段文案格式两处同步；③ stops/segments 长度对齐（len(segs)==len(stops)-1）有测试断言。

#### `tools/imagegen.py`（66 行）——占位图
- **作用**：PIL 渐变占位卡（md5 稳定配色）。
- **上游**：config（IMAGE_DIR）。**下游**：search._placeholder；conftest（monkeypatch imagegen.IMAGE_DIR）；render_verify/e2e_step4 脚本；test_pdf/test_pdf_html。
- **注意点**：search 在其函数内动态 import IMAGE_DIR 以兼容测试 monkeypatch，改 import 方式前先看这个模式。

#### `tools/resilience.py` 之外全部公共入口汇总
| 函数 | 调用方 |
|---|---|
| `search_guides/search_images/search_city_covers` | team.py |
| `query_tickets/score_and_select` | team.py |
| `query_hotels/score_and_select/enrich_hotels` | team.py |
| `query_weather/near_term_dates` | team.py |
| `compute_route_plan/review_route_plan/merge_review` | team.py |
| `run_channel_subagent` | team.py（8 通道） |

---

### 4.5 `tripmate/mocks/data.py`（249 行）——模拟知识库
- **作用**：§7 降级方案 mock 数据生成器 + 7 城市知识库（CITY_KB）。
- **上游**：无。**下游**：weather、tickets、route、search、hotels、planning（kb_for_city/spot_ticket_price）、team、chatter（expand_dates）；test_mocks_data/test_regression_fixes。
- **契约**：各 mock 函数键集与对应真实工具返回**同构**（这是降级路径无缝替换的前提），见下表；**唯一例外**：`mock_guide_digest` 行是 spots/foods/routes/warnings 结构化四元，而真实 Tavily 行是 raw_answer/raw_titles/raw_urls——由 team.py:309 structure_guide_rows 吸收。
- **修改注意点**：① 加/改 mock 键必须对照真实工具键集逐项核对；② `spot_ticket_price("汉中石门栈道")==70` 等 magic number 有测试断言；③ 陕南三城收录 + `REGION_ALIAS["陕南"]=="汉中"` 是 test_mocks_data 的验收点。

---

### 4.6 PDF 渲染（双链路）

**链路关系**：`team.py:876` → `pdf_gen.build_pdf(profile, run_id, template, on_fallback)` → 主路径 `pdf_html.render`（Jinja2 + Chromium）；任何异常或 `PDF_RENDERER=reportlab` → 降级 `pdf_templates` reportlab cartoon（恒用 cartoon，不随主题）。

#### `pdf_gen.py`（55 行）——PDF 入口与降级仲裁
- **作用**：`build_pdf`（主路径 html + 异常降级 + on_fallback 回调）与 `render_reportlab`。
- **上游**：config（PDF_RENDERER）、models、status；懒加载 pdf_html / pdf_html.themes / pdf_templates。**下游**：team；test_pdf/test_pdf_themes/test_templates；render_verify 脚本。
- **契约**：`template` 参数可能是 HTML 主题名或旧模板名 → resolve_theme 容错；降级恒用 cartoon。
- **修改注意点**：换默认渲染路径 = 改 PDF_RENDERER 语义，联动 README/启动指南文档表。

#### `pdf_html/__init__.py`（11 行）
- 门面：`from .engine import render`。import 本包不要求 playwright/pymupdf 已装（engine 懒加载）。

#### `pdf_html/engine.py`（136 行）——Chromium 渲染 + pymupdf 后处理
- **作用**：Playwright 无头 Chromium 三段渲染（封面/正文/封底，捆绑 Chromium 失败退系统 Edge）+ pymupdf 合并/盖页码/书签/元数据/原子落盘。
- **上游**：models、status、context、themes；懒加载 jinja2/playwright/pymupdf。**下游**：pdf_html/__init__。
- **契约**：`cfg` 键来自 themes.py 并被 engine 消费：`css, cover, back, page_num, page_num_last, titles, display_name`；渲染三元组 = `(cfg["cover"], "body.html.j2", cfg["back"])`（:127-129，**body 模板三主题硬编码共用**，新增主题必须配 cover/back）；`cfg["titles"]` 用于书签定位；模块级 `_LOCK` 串行化。
- **修改注意点**：改 titles → 书签定位 + test_pdf_themes 行政标题断言；改书签/页码规则要跑 test_pdf_html（get_toc/页码格式断言）。

#### `pdf_html/context.py`（652 行）——TravelProfile → Jinja2 上下文
- **作用**：数据整形 + 确定性 SVG 图表，排版交给模板。变量名与 j2 模板占位符逐项对应（basic/chapters/sec_titles/snapshot/alarms/gantt/weather/orders/days/budget/gallery/hotels/foods/warnings/sources/defaults/generated_at/principles/checklist/pretrip/emergencies/theme_name/tl_colors/cover/back）。
- **上游**：config（OUTPUT_DIR）、models、**planning.compute_budget**、**tools.weather（outfit_advice/weather_emoji）**、themes。**下游**：engine；conftest（monkeypatch OUTPUT_DIR）。
- **契约**：`output_path` 命名 `行程计划_{目的地}_{run_id[:8]}.pdf`（与 base.py 同规则）；`budget.donut|safe`/`budget.stack|safe`/`css|safe` 依赖 autoescape + 自产 SVG；实拍图过滤文案匹配（:79）；`_DEST_BAD` 目的地下拉消毒（:26-27 注释：与 base.py:218 保持一致，**双份维护**）。
- **修改注意点**：① 改任何 context 键名 = 改 7 个 j2 模板消费处；② `days[i].theme` 回填（:357-359）；③ foods 三态渲染（notes/grid/fallback）；④ draft.html 用的是**另一套**上下文（见 templates 卡），不要混用。

#### `pdf_html/themes.py`（114 行）——主题注册表
- **作用**：THEMES 纯数据（lushu 默认/elegant/executive）+ `_validate`（注册时校验 12 章 + 3 甘特色）+ `get_theme`（未知名抛错）+ `resolve_theme`（None/未知/旧名 "cartoon" → lushu）+ `list_themes`。
- **上游**：无。**下游**：pdf_gen、engine、context、gateway/app.py（/api/templates 与 WS 模板切换）；test_pdf_themes。
- **契约**：每主题键 `css, display_name, description, cover, back, chapter_labels, titles, palette[6], gantt_colors[3], svg_*, occ_ok/warn, page_num, page_num_last`；三主题 titles 各 12 个。
- **修改注意点**：改主题名五处联动（速查表第 5 行）；改 chapters/titles 数量 → `_validate` 断言 + body.html.j2 的位置索引（chapters[0..11]）；新增主题必须配齐 css + 封面/封底模板。

#### `pdf_html/templates/`（7 个 j2 + 3 个 css）
- `body.html.j2`：三主题共用正文（约 17.5KB 最大文件），按位置索引 chapters[0..11]/sec_titles[0..11]。
- `cover.html.j2` / `cover_elegant.html.j2` / `cover_executive.html.j2`：封面（消费 context 的 `cover` 对象：badge/countdown/cover_uri/en_line/foot/motto/subtitle/title）。
- `back.html.j2` / `back_elegant.html.j2` / `back_executive.html.j2`：封底。
- `lushu.css` / `elegant.css` / `executive.css`（各约 17KB）：文件头注释明确 elegant/executive 以 lushu 为基底复制，**类集合与 CSS 变量名保持契约一致**，仅改变量值与末尾主题覆盖块；变量 `--red*`（语义=主题主色位，executive 里映射为藏蓝）/`--gold*`/`--navy`/`--paper` 等。
- **修改注意点**：改 context 键名 → 7 模板同步改占位符；改 CSS 类名 → 先确认该 class 不在另两个 css 里共用；@page 规则由各段模板内联注入（css 只含类样式）。

#### `pdf_templates/__init__.py`（31 行）——reportlab 注册表
- **作用**：`_TEMPLATE_CLASSES=[CartoonTemplate]`、`REGISTRY`、`DEFAULT_TEMPLATE="cartoon"`、`get_template`（未知名抛 ValueError→调用方回退）、`list_templates`。
- **下游**：pdf_gen、gateway/app.py（/api/templates 降级引擎列表）、make_template_samples；test_templates。

#### `pdf_templates/base.py`（650 行）——reportlab 公共积木
- **作用**：字体注册、emoji 栅格化、esc、样式/表格积木、图片积木（crop_43/day_strip/image_cell）、`route_day_block`、封面 PIL 绘制 `make_cover`。类属性即主题配色。
- **上游**：config（OUTPUT_DIR）、models、planning.compute_budget。**下游**：cartoon；conftest（monkeypatch OUTPUT_DIR/CROP_DIR）；test_templates。
- **契约**：`route_day_block`（:428-502）与 context.py `_seg_text` 逐项对齐（双份维护）；实拍图过滤（:147,568）；`_DEST_BAD`（:218）；输出命名（:218-219）。
- **修改注意点**：conftest 钉 OUTPUT_DIR/CROP_DIR 是测试隔离点；改积木签名同步 cartoon 调用处与 test_templates。

#### `pdf_templates/cartoon.py`（457 行）——唯一注册的 reportlab 模板
- **作用**：`CartoonTemplate`（珊瑚粉/暖黄）+ `_ClassicLayout`（原 classic.py 逐字并入，name="classic" 但**不注册**）。
- **上游**：models、**tools.weather（outfit_advice/weather_emoji——与 context.py 重复依赖同一函数）**、base。**下游**：注册表。
- **注意点**：与 context.py 的共享口径（weather 函数/compute_budget/实拍图过滤/路线段文案/输出命名）**改任一侧另一侧 PDF 不一致**；`_SECTION_EMOJI` 与 section 覆写是主题风格点。

---

### 4.7 `static/`（前端三件套 + PWA）

#### `static/index.html`（121 行）
- **作用**：单页骨架（左栏：画像/订单/记忆/Token；中部：聊天 + 徽章 + 通道灯 + 连接灯；右栏：Agent 时间线；登录浮层）。
- **契约**：全部 DOM id 供 app.js `$()` 使用（style.css:3 注释声明选择器契约）；8 个灯 `data-channel`（:68-75）；6 个徽章 `data-agent`（:60-65）；`style.css?v=2`（:10）与 `app.js?v=3`（:119）**手工缓存击破版本**；manifest/apple-touch-icon 引用。
- **修改注意点**：改任何 id/class 三处同步（速查表第 9、12 行）。

#### `static/app.js`（623 行）
- **作用**：原生 JS 前端：鉴权、WS 票据、心跳 25s、退避重连、离线 outbox、消息分发渲染、灯态、ETA、模板选择、会话管理、分享、记忆管理。
- **契约**：与网关的 HTTP/WS 契约全部在 3.1 节；`SUB_PARENT`（:435）是通道名的前端单一来源；localStorage 键 `tm_token/tm_sid/tm_style`；handleMsg switch（:271-317）；时序侧面：前端心跳 25s / 徽章 35s 熄灭 / 退避重连。
- **修改注意点**：改发送/接收字段必须对着网关逐项改；新增通道灯同步 index.html；`tm_token` 过期逻辑与 auth.py TTL 耦合。

#### `static/style.css`（270 行）
- **作用**："明媚阳光"主题（CSS 变量 + 三栏 grid + ≤700px 竖屏适配）。
- **注意点**：文件头 :3 明确"app.js 依赖的 id/class 全部保留"——改 class 名同时打断 CSS 与 JS。

#### `static/manifest.webmanifest`、`static/icons/icon-192.png`、`static/icons/icon-512.png`
- PWA 配置（start_url "/"、standalone）与两个图标（被 index.html:9 与 manifest 双重引用）。无代码含义。

---

### 4.8 `scripts/`（12 个，统一模式：`sys.path.insert(0, 项目根)` 后 import tripmate.*）

| 脚本 | 作用 / 链路 | 关键契约 |
|---|---|---|
| `e2e_step1_chatter.py` | 冒烟①：Chatter 抽取 + 启动判定（真实 LLM）；入口 `Session.handle_user_message` | 12 项抽取检查点 |
| `e2e_step2_collect.py` | 冒烟②：四 Agent 协同 → 草稿；等 team_events "draft_ready" | 读黑板 9 个分区；compute_budget 键 |
| `e2e_step3_full.py` | 冒烟③：改预算→检查点重跑→反馈→确认→配图+PDF 全流程 | `wait_event_liveness` 双通道静默判死；断言 STATUS_CHECKPOINT |
| `e2e_step4_finalize.py` | 冒烟④（无 LLM）：手工黑板 → `_deliver_final` → PDF/订单/完成事件 | **import team 私有符号**（TeamContext/TeamRunner/TeamState/_deliver_final）；订单 type∈{车票,酒店} |
| `e2e_shannan_case.py` | 用户场景 e2e（成都→陕南 3 天 2 夜）；`--direct-confirm` 旁路直调 submit_feedback | team_events 的 error 也是终止信号 |
| `cleanup_outputs.py` | outputs 清理 CLI（默认 dry-run，`--apply` 才真删，中止退出码 2） | plan 字段与 maintenance 契约一致 |
| `make_template_samples.py` | reportlab 注册表全模板渲染样张 → docs/references/samples/ | **import tests.test_pdf._profile_bb**（跨目录依赖测试夹具） |
| `probe_collect_quality.py` | 诊断：8 路 Tavily + structure_guide_rows + 封面图 | 返回键契约 |
| `probe_max_tokens.py` | 一次性诊断：max_tokens 8192 vs 16384 | digest._STRUCT_PROMPT |
| `render_verify.py` | reportlab 视觉验收 → outputs/pdf_baseline/ PNG | **`exec()` 拼接 tests/test_pdf.py 前段取 _profile_bb（极脆弱）** |
| `render_html_verify.py` | HTML 三主题视觉验收 + 结构断言（章节标题/高德链接/书签） | **import tests 夹具**（test_pdf_html._rich_profile、test_pdf._routes_profile_bb） |
| `ws_status_smoke.py` | ⚠️ **已失效**：WS 冒烟起真实服务验证 5 项 | URL 只有 sid 无 ticket（:26），网关 :590 要求票据 → 5 项全挂。修需补 /api/ws-ticket 换票逻辑 |

**通用注意点**：所有 e2e/probe/render 脚本直接 import 源模块甚至私有符号与测试夹具——改源模块签名/测试夹具默认值会同时打断脚本；`render_verify.py` 的 exec() 拼接对 test_pdf.py 文件结构敏感，改 test_pdf.py 前先看这两个脚本。

---

### 4.9 `tests/`（29 个文件）

#### `tests/conftest.py`（18 行）
- **作用**：autouse 夹具 `_isolate_outputs`，把 PDF/占位图产物钉到 `tmp_path/outputs`。
- **契约**：monkeypatch 四个点——`pdf_html.context.OUTPUT_DIR`、`pdf_templates.base.OUTPUT_DIR/CROP_DIR`、`imagegen.IMAGE_DIR`（:15-18）。断言 `Path(path).parent.name == "outputs"` 的用例不受影响（临时目录同名）。
- **注意点**：改这三个模块的输出常量**名字**会让隔离静默失效、pytest 污染真实 outputs；maintenance 测试自带隔离夹具，不在此列。

#### 测试 → 覆盖模块对照表（共 228 个测试，`python -m pytest tests/ -q` 2026-09-23 实测）

| 测试文件 | 覆盖 | 硬编码契约（举例） |
|---|---|---|
| `test_blackboard.py` | blackboard、planning.analyze_impact、team._changed_fields | writer 名；`missing_required()==["出发地","目的地","游玩天数"]`；影响集 |
| `test_chat_delivery_persistence.py` | persistence、session、gateway.app（_chat/_sender/_replay_snapshot/_cancel_current_chat） | `junk-` 前缀；`_skey="用户名/sid"`；sender 按 seq 去重 |
| `test_collection_workers.py` | digest.structure_guide_rows、team.make_researcher_tools | LLM JSON 载荷 `{"rows":[...]}`；通道名；工具回执 `{"status":"written\|reused\|error",...}` |
| `test_frontend_state.py` | gateway._replay_snapshot/_sender、search 熔断、app_window | 补播顺序（sub_states 在 profile 后）；_COMMONS_DISABLED |
| `test_guide_digest.py` | models.GuideDigestItem raw 字段 | 7 个 raw 键 + 结构化四元 |
| `test_llm_fallback.py` | llm._FallbackClient | 冷却常量；工具名 save_travel_info |
| `test_maintenance.py` | maintenance 全链 | plan 字段；`MaintenanceAborted`（match "损坏"/"分享索引"） |
| `test_mocks_data.py` | mocks.data、chatter.REGION_ALIAS | 三城收录；spot_ticket_price 数值；陕南→汉中 |
| `test_multiuser.py` | auth、gateway 全部 HTTP、会话隔离、memory、mcp 池隔离、_build_team | 401 面清单；share_id 正则；池键 "amap"/"12306" |
| `test_new_features.py` | search._guide_queries/_host_rank、app._session_title、chatter 两个函数、llm.check_budget | 8 路查询名；标题格式"汉中 · 收集需求中"；monkeypatch TOKEN_LIMIT |
| `test_pdf.py` | pdf_gen.build_pdf 全部路径 + _deliver_final 降级 | **夹具 _profile_bb/_routes_profile_bb 被 4 脚本 + 3 测试复用**；产物命名；STATUS_FALLBACK |
| `test_pdf_html.py` | pdf_html.render | 章节文案；页码 `"{n} / {n}"`；uri.amap.com；get_toc()；`from test_pdf import _profile_bb` |
| `test_pdf_themes.py` | pdf_html.themes + 三主题渲染 + /api/templates | 主题名；resolve_theme 回退；executive 文案；cartoon 仍在 reportlab 列表 |
| `test_planning.py` | planning 三个函数 + tickets/hotels 打分 | 预算数值口径 3706/858/210；预警文案 |
| `test_real_channel_paths.py` | search 真实通道、weather 串联 | WeatherConfig.GEO_URL/BASE_URL；days 键序 |
| `test_regression_fixes.py` | route 全模块、_normalize_hotels、_fallback_draft、mcp 握手并发 | BUS_PRICE_PER_KM；len(segs)==len(stops)-1 |
| `test_relay_view.py` | chatter._profile_view、session.relay_team_event、app._sender | view 键名；RELAY_TIMEOUT_S；降级文案含"卡片" |
| `test_resilience_hardening.py` | resilience、JobBoard、mcp_client 池、_sub_notify | ServiceUnavailable；池键 "12306"；train_session() |
| `test_route.py` | tools/route 全模块 + _ensure_sections/_deliver_final 路线双保险 | uri.amap.com 格式；800m/5000m/30000m 阈值；RouteConfig.BUDGET_S |
| `test_second_planning.py` | session 三组正则、handle_user_message 兜底、TeamRunner.start | 回执文案；receipt.status 枚举；eta_min [5,10]/[5,15] |
| `test_selector.py` | team selector 状态机 + start/submit_feedback 契约 | SPEAKER 键全集；agent 名；≤3 轮反馈 |
| `test_stability_hardening.py` | _FallbackConnectionError 重试、session 重建、start 幂等、_replay 分级 | "再发一次"文案；换城清笔记分区 |
| `test_stop_dates.py` | chatter.DEFAULTS/ensure_travel_dates、make_researcher_tools、cancel | 必问出行时间；工具名集合；RES_COLLECT→MCP_COLLECT |
| `test_subagents.py` | run_channel_subagent | 工具名 query_channel_data；notify 序列；回调异常吞掉 |
| `test_templates.py` | pdf_templates 注册表 | get_template(None).name=="cartoon"；元数据 |
| `test_tool_call_guard.py` | session._missed_tool_call + chatter.clean_reply | 四种工具文本化变体；正则负向 |
| `test_weather_dates.py` | near_term_dates | 预报窗日期 |
| `test_zombie_team.py` | _stream_team/_abort_team_stream | _runtime.stop 顺序；生成器 aclose |

**测试注意点**：① `test_pdf.py` 的两个夹具是 3 个测试 + 3 个脚本的公共依赖，改默认值会连锁改期望（如 test_planning 的 3706/858/210）；② 测试大量直接 import 被测模块的**私有函数/常量**——重命名 = 测试红一片，但这也是故意留的契约哨兵；③ 跑测试用 `python -m pytest tests/ -q`（228 项）。

---

### 4.10 文档与数据文件

#### `README.md` / `启动指南.md`
- **作用**：快速开始 + 架构总览 + 验收项对应 + 降级矩阵 / 安装配置 + 验收用例 + FAQ + 多用户 + outputs 清理。
- **注意点**：两份文档有**手工维护的数字**（测试计数、脚本清单），改代码导致数字变化时同步；`.env` 配置表与 `.env.example` 对齐。均已于 2026-09-23 更新测试计数为 228。

#### `data/users.json`、`data/secret.key`、`data/memory/moring.json`、`sessions/moring/*.json`
- **作用**：账号库/令牌签名密钥/示例用户记忆/示例会话（演示数据）。
- **注意点**：均被 .gitignore 排除（含敏感信息）；schema 见 3.7 节；删除 data/secret.key 会令全体令牌失效（无害，重新签发即可）。

#### 生成物目录（不做文件卡）：`outputs/`（PDF/配图/crops/baseline）、`logs/`（tripmate.log 审计）、`.pytest_cache/`、`learning/`（参考 PDF）、`docs/references/samples/`（模板样张）
- 全部由程序生成或二进制参考资料；schema 契约已并入对应源文件的卡片（maintenance 扫描 outputs 的规则见其卡）。
- 上级目录的 `TripMate-backup-20260919/` 是**历史备份，禁止编辑**；排查"代码为什么变了"时可对照，但它不是工程的一部分。

---

## 5. 分类修改检查清单

> 改完后逐项打勾；任何一项不确定就先 grep 共享词汇（第 3 节）再动手。

### 改 WS 消息字段/新增消息类型
- [ ] `status.py` emit 结构 + `gateway/app.py` 发送方字段
- [ ] `static/app.js` handleMsg switch + 对应渲染函数
- [ ] `test_chat_delivery_persistence.py` / `test_frontend_state.py` / `test_resilience_hardening.py` 断言
- [ ] 新增 kind：前端默认分支、css 样式（如需要）、第 3.1 节词表更新

### 改 models / 黑板字段
- [ ] `models.py` 字段 + `blackboard.py` apply_* 特判（新增 detail 子对象要扩展 hotel 特判）
- [ ] `persistence.py` 快照、`maintenance.py` 引用扫描字段清单
- [ ] 前端 `renderProfile`、`templates/draft.html`、`pdf_html/context.py`、`pdf_templates/base.py`
- [ ] 相关测试 fixture；`python -m pytest tests/test_blackboard.py tests/test_pdf.py -q`

### 新增/改工具（外部通道）
- [ ] 工具返回 dict 键 ↔ `team.py` 收割解析 ↔ `models` 候选模型 ↔ `mocks/data.py` 同构 mock
- [ ] MCP：config 新配置 + `mcp_client.py` 加工厂 + 工具内调用（三处）
- [ ] 关键词改动 → `test_route.py`、`test_resilience_hardening.py`
- [ ] `ALLOW_MOCK_FALLBACK` 降级路径可用；降级文案

### 新增/改 PDF 主题
- [ ] `themes.py` THEMES + `_validate`（12 章/3 色）过
- [ ] `pdf_html/templates/` 配 cover/back（body 共用）+ css
- [ ] `static/app.js` 默认值/回显、`test_pdf_themes.py`、网关 `/api/templates`
- [ ] 第 3.5 节词表更新

### 改配置项
- [ ] `config.py` 默认值合理（未配置走降级）
- [ ] `.env.example` 补键（含注释）
- [ ] README/启动指南配置表
- [ ] 测试 monkeypatch 的类属性名是否受影响

### 改前端
- [ ] id/class 三处一致（index.html / style.css / app.js）
- [ ] `index.html` 缓存版本号递增
- [ ] 新通道灯：index.html 灯位 + app.js SUB_PARENT + 后端 emit channel
- [ ] WS 发送字段与网关逐项对照

### 改 prompts / 协议标记词
- [ ] `team.py` 标记词常量 ↔ `_selector` 判定 ↔ `prompts.py` 流程描述三处一致
- [ ] `test_selector.py` 的 SPEAKER 映射测试
- [ ] 字段清单 ↔ chatter 工具 docstring ↔ models

### 改 mock 数据
- [ ] 键集与对应真实工具返回逐项核对
- [ ] `test_mocks_data.py` / `test_regression_fixes.py` 数值断言
- [ ] team/digest 的结构化吸收逻辑（mock_guide_digest 的结构四元 ≠ 真实 raw 键）

---

## 6. 维护约定与已知问题

### 何时必须更新本文件
1. 新增/删除/重命名任何文件 → 更新第 4 节；
2. 改动任何第 3 节共享词汇 → 更新词表 + 第 2 节速查表；
3. 发现新的"改 X 忘 Y"实例 → 补进速查表（每条都应是踩过坑的）；
4. 修复已知问题或测试数量变化 → 更新对应数字。

### 已知问题记录（2026-09-23）
| 问题 | 位置 | 影响 | 状态 |
|---|---|---|---|
| WS 冒烟脚本失效：未带 ticket，5 项检查必挂 | `scripts/ws_status_smoke.py:26` vs `gateway/app.py:618` | 冒烟不可用 | 待修（补 /api/ws-ticket 换票） |
| `render_verify.py` 用 `exec()` 拼接 `tests/test_pdf.py` 源码取夹具 | `scripts/render_verify.py:24` | test_pdf.py 改结构即断 | 待重构（改为直接 import） |
| `docs/pdf-template-tasks.md` 进度记录停留在 2026-09-06 路线 A 转正，3.3 Agent 自动推荐模板标注"可选未做" | docs | 文档与现状的偏差（不影响代码） | 记录在案 |
| McpConfig.TIMEOUT_S/RETRIES/RETRY_DELAY_S 弃用值仍保留 | config.py:70-74 | 新代码误用 | 注释已警示 |
| Tavily Key 返回 HTTP 432（失效/配额用尽）：8 路攻略检索全败 | `tools/search.py` `_tavily` | 攻略降级为内置知识库（已标注参考值）；死 Key 三轮重试白耗约 50s/次规划 | 待用户更换 Key；"4xx 快速失败"已列入运行时间优化方案 |
| 12306 预售窗外查询回落今日（10-01 超出预售窗口） | tools/tickets.py | 订单链接内日期为查询日，用户下单前须自行核对 | 团队已按 reference_only 标注，属预期降级 |
| `submit_draft_feedback(confirmed=true)` 漏传 feedback 触发一次 pydantic 校验重试 | chatter 工具 schema | 模型自愈（约 +7s），用户无感知 | 列入优化方案候选 |

### 2026-09-23 修复记录（e2e 实测驱动，均有回归测试）
| 修复 | 位置 | 根因 |
|---|---|---|
| nudge 放行未完成的必要工具调用（点名 start_planning/submit_draft_feedback） | `session.py` handle_user_message 的 Reflect 兜底 | 原指令"不要再次调用任何工具"逼停模型补调，规划静默死亡 |
| `_START_INTENT` 增补"生成/制定…行程/方案"与"规划团队…出"两个分支 | `session.py:24-42` | 新主模型 nudge 总结措辞无"开始/启动"字样，兜底未命中 |
| 工具轮连续空 content 后放行一次无 tools 请求 | `llm.py` `_FallbackClient.create` | 新主模型（deepseek-v4-flash-0731 聚合端点）反思轮持续空回复，团队路径无兜底直接猝死 |
| 阶段对 "Reflect on tool use" 异常单次重试（熔断不重试） | `team.py` `_phase_loop` | 连接重置类漏网时的团队侧保险 |
| 偏好沉淀完成推 `memory` WS 消息 + 定稿刷新会话标题 | `gateway/app.py`、`static/app.js` | 面板竞态（停在"暂无沉淀"）、标题停留"收集需求中" |
| 重启后落盘会话补进下拉列表 | `gateway/app.py` `list_sessions` | 内存注册表重启即丢，历史对话 UI 不可达 |
| 画像/记忆行程日期改显起止范围 | `static/app.js`、`memory.py` | 三天用 ` ~ ` 链式显示语义含混 |

### 长期改进建议（本次不做）
- **契约集中化**：把标记词/通道名/WS kind 等共享词汇收拢到单一常量模块（如 `tripmate/contracts.py`），让重复定义变成 import，把"文本约定"升级为"编译期检查"；
- 模板/CSS 的共享规则（实拍图过滤文案、`_DEST_BAD`、路线段文案）同理收敛为共用函数，消除双份维护。
